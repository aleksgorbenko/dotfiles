#!/bin/bash
#
# cdf
#
# A command to quickly commit and push the dotfiles directory

if [ -n "$1" ]; then
  MSG=$1
else
  MSG="Routine changes"
fi
echo $MSG
DIR=$(pwd)
cd $ZSH
git secret hide
git add -A
git commit -m "$MSG"
git push
cd $DIR